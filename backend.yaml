- name: Configure backend Server
  hosts: backend
  become: yes
  vars:
    mysql_root_password: ExpenseApp@1
    login_host: mysql.jioairlines.online
  tasks:
    - name: disable default nodejs
      ansible.builtin.command: dnf module disable nodejs -y

    - name: enable nodejs 24
      ansible.builtin.command: dnf module enable nodejs:24 -y

    - name: Install nodejs and mysql
      ansible.builtin.package:
        name: "{{ item }}"
        state: present
      loop:
        - nodejs
        - mysql

    - name: Install unzip (needed for unarchive)
      ansible.builtin.package:
        name: unzip
        state: present

    - name: create expense user
      ansible.builtin.user:
        name: expense

    - name: create app directory
      ansible.builtin.file:
        path: /app
        state: directory

    - name: download the backend code
      ansible.builtin.get_url:
        url: https://expense-builds.s3.us-east-1.amazonaws.com/expense-backend-v2.zip
        dest: /tmp/backend.zip

    - name: extract the code
      ansible.builtin.unarchive:
        src: /tmp/backend.zip
        dest: /app
        remote_src: yes

    - name: install dependencies
      ansible.builtin.command: npm install
      args:
        chdir: /app

    - name: copy backend service
      ansible.builtin.copy:
        src: backend.service
        dest: /etc/systemd/system/backend.service

    - name: ansible to install pymysql
      ansible.builtin.pip:
        name:
          - PyMySQL
          - cryptography
        executable: pip3.9

    - name: import mysql data
      community.mysql.mysql_db:
        login_user: root
        login_password: "{{ mysql_root_password }}"
        login_host: "{{ login_host }}"
        target: /app/schema/backend.sql
        name: all
        state: import

    - name: daemon reload
      ansible.builtin.systemd_service:
        daemon_reload: true
        enabled: true
        state: restarted
        name: backend

# netstat -nltp         application running or not checking

     # [Unit]
# Description=Expense Backend Service
# After=network.target

# [Service]
# User=expense
# WorkingDirectory=/app
# ExecStart=/usr/bin/node /app/index.js
# Restart=always
# Environment=DB_HOST=mysql.jioairlines.online
# Environment=DB_USER=root
# Environment=DB_PASSWORD=ExpenseApp@1

# [Install]
# WantedBy=multi-user.target


# nc -zv mysql.jioairlines.online 3306
# dig mysql.jioairlines.online


 #  In interviewr aksed you what type of modules you are using in ansible..?
#  i usually using modules are below 
                  #  1 package 
                  #  2 copy
                  #  3  user 
                  #  4 get url
                  #  5 unarchive
                  #  6 file 
                  #  7 mysql 
                  #  8 pip 
                  #  9 service 